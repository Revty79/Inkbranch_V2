# Drizzle Migrations

Generated SQL migration files are written to this directory by `npm run db:generate`.

This folder intentionally starts empty until schema tables are introduced in later prompts.
