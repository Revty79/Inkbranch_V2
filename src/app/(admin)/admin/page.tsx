export default function AdminPage() {
  return (
    <main className="page-shell">
      <h1>Admin</h1>
      <p>Admin surface for run inspection, diagnostics, and observability.</p>
      <p>
        This area must not own canon decisions, planner logic, or runtime
        mutation behavior.
      </p>
    </main>
  );
}
