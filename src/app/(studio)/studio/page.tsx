export default function StudioPage() {
  return (
    <main className="page-shell">
      <h1>Studio</h1>
      <p>
        Studio surface for authored world, book, and bible input management.
      </p>
      <p>
        This area must orchestrate requests only and must not contain planner or
        runtime engine logic.
      </p>
    </main>
  );
}
