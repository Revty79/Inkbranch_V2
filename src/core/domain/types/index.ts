/**
 * Domain type placeholders for authored truth boundaries.
 * Real contracts are introduced in prompt 03.
 */
export interface DomainTypesPlaceholder {
  readonly tag: "domain-types-placeholder";
}
